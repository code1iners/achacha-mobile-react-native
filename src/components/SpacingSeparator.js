import React from "react";
import styled from "styled-components/native";

const Container = styled.View`
  padding: 10px;
`;

const SpacingSeparator = () => <Container />;

export default SpacingSeparator;
